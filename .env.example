# PostgreSQL connection string used by Prisma and the app
DATABASE_URL="postgresql://postgres:password@localhost:5432/public"

# Secret key used to sign JWT tokens
JWT_SECRET="replace_with_a_long_random_secret"
